export const environment = {
  production: true,
  // URL del backend desplegado en Railway. Cámbiala si tu dominio del
  // servicio backend es distinto (Settings -> Networking del servicio backend).
  apiUrl: 'https://tfg-peluqueria-production.up.railway.app/api/v1'
};
